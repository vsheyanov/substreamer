diff --git a/node_modules/react-native/ReactAndroid/src/main/java/com/facebook/react/fabric/mounting/mountitems/IntBufferBatchMountItem.kt b/node_modules/react-native/ReactAndroid/src/main/java/com/facebook/react/fabric/mounting/mountitems/IntBufferBatchMountItem.kt
index 221171f..8749e7f 100644
--- a/node_modules/react-native/ReactAndroid/src/main/java/com/facebook/react/fabric/mounting/mountitems/IntBufferBatchMountItem.kt
+++ b/node_modules/react-native/ReactAndroid/src/main/java/com/facebook/react/fabric/mounting/mountitems/IntBufferBatchMountItem.kt
@@ -193,150 +193,53 @@ internal class IntBufferBatchMountItem(
   override fun isBatchEmpty(): Boolean = intBufferLen == 0
 
   override fun toString(): String {
+    // Return a compact summary to avoid OOM from enumerating large mount batches.
+    // The original implementation built a full string for every operation, which
+    // causes OutOfMemoryError when crash reporters call toString() on large batches.
     try {
-      val s = StringBuilder()
-      s.append(String.format(Locale.ROOT, "IntBufferBatchMountItem [surface:%d]:\n", surfaceId))
+      var creates = 0
+      var deletes = 0
+      var inserts = 0
+      var removes = 0
+      var propUpdates = 0
+      var stateUpdates = 0
+      var layoutUpdates = 0
+      var paddingUpdates = 0
+      var overflowUpdates = 0
+      var eventEmitterUpdates = 0
       var i = 0
-      var j = 0
       while (i < intBufferLen) {
         val rawType = intBuffer[i++]
         val type = rawType and INSTRUCTION_FLAG_MULTIPLE.inv()
         val numInstructions =
             (if ((rawType and INSTRUCTION_FLAG_MULTIPLE) != 0) intBuffer[i++] else 1)
-        for (k in 0 until numInstructions) {
-          when (type) {
-            INSTRUCTION_CREATE -> {
-              val componentName = (objBuffer[j++] as String?).orEmpty()
-              val fabricComponentName =
-                  FabricNameComponentMapping.getFabricComponentName(componentName)
-
-              j += 3
-              s.append(
-                  String.format(
-                      Locale.ROOT,
-                      "CREATE [%d] - layoutable:%d - %s\n",
-                      intBuffer[i++],
-                      intBuffer[i++],
-                      fabricComponentName,
-                  )
-              )
-            }
-            INSTRUCTION_DELETE ->
-                s.append(String.format(Locale.ROOT, "DELETE [%d]\n", intBuffer[i++]))
-            INSTRUCTION_INSERT ->
-                s.append(
-                    String.format(
-                        Locale.ROOT,
-                        "INSERT [%d]->[%d] @%d\n",
-                        intBuffer[i++],
-                        intBuffer[i++],
-                        intBuffer[i++],
-                    )
-                )
-            INSTRUCTION_REMOVE ->
-                s.append(
-                    String.format(
-                        Locale.ROOT,
-                        "REMOVE [%d]->[%d] @%d\n",
-                        intBuffer[i++],
-                        intBuffer[i++],
-                        intBuffer[i++],
-                    )
-                )
-            INSTRUCTION_UPDATE_PROPS -> {
-              val props = objBuffer[j++]
-              val propsString =
-                  if (FabricUIManager.IS_DEVELOPMENT_ENVIRONMENT) (props?.toString() ?: "<null>")
-                  else "<hidden>"
-              s.append(
-                  String.format(Locale.ROOT, "UPDATE PROPS [%d]: %s\n", intBuffer[i++], propsString)
-              )
-            }
-            INSTRUCTION_UPDATE_STATE -> {
-              val state: StateWrapper? = objBuffer[j++] as StateWrapper?
-              val stateString =
-                  if (FabricUIManager.IS_DEVELOPMENT_ENVIRONMENT) (state?.toString() ?: "<null>")
-                  else "<hidden>"
-              s.append(
-                  String.format(Locale.ROOT, "UPDATE STATE [%d]: %s\n", intBuffer[i++], stateString)
-              )
-            }
-            INSTRUCTION_UPDATE_LAYOUT ->
-                s.append(
-                    String.format(
-                        Locale.ROOT,
-                        "UPDATE LAYOUT [%d]->[%d]: x:%d y:%d w:%d h:%d displayType:%d" +
-                            " layoutDirection:%d\n",
-                        intBuffer[i++],
-                        intBuffer[i++],
-                        intBuffer[i++],
-                        intBuffer[i++],
-                        intBuffer[i++],
-                        intBuffer[i++],
-                        intBuffer[i++],
-                        intBuffer[i++],
-                    )
-                )
-            INSTRUCTION_UPDATE_PADDING ->
-                s.append(
-                    String.format(
-                        Locale.ROOT,
-                        "UPDATE PADDING [%d]: top:%d right:%d bottom:%d left:%d\n",
-                        intBuffer[i++],
-                        intBuffer[i++],
-                        intBuffer[i++],
-                        intBuffer[i++],
-                        intBuffer[i++],
-                    )
-                )
-            INSTRUCTION_UPDATE_OVERFLOW_INSET ->
-                s.append(
-                    String.format(
-                        Locale.ROOT,
-                        "UPDATE OVERFLOWINSET [%d]: left:%d top:%d right:%d bottom:%d\n",
-                        intBuffer[i++],
-                        intBuffer[i++],
-                        intBuffer[i++],
-                        intBuffer[i++],
-                        intBuffer[i++],
-                    )
-                )
-            INSTRUCTION_UPDATE_EVENT_EMITTER -> {
-              j += 1
-              s.append(String.format(Locale.ROOT, "UPDATE EVENTEMITTER [%d]\n", intBuffer[i++]))
-            }
-            else -> {
-              FLog.e(TAG, "String so far: $s")
-              throw IllegalArgumentException(
-                  "Invalid type argument to IntBufferBatchMountItem: $type at index: $i"
-              )
-            }
-          }
+        when (type) {
+          INSTRUCTION_CREATE -> { creates += numInstructions; i += numInstructions * 2 }
+          INSTRUCTION_DELETE -> { deletes += numInstructions; i += numInstructions }
+          INSTRUCTION_INSERT -> { inserts += numInstructions; i += numInstructions * 3 }
+          INSTRUCTION_REMOVE -> { removes += numInstructions; i += numInstructions * 3 }
+          INSTRUCTION_UPDATE_PROPS -> { propUpdates += numInstructions; i += numInstructions }
+          INSTRUCTION_UPDATE_STATE -> { stateUpdates += numInstructions; i += numInstructions }
+          INSTRUCTION_UPDATE_LAYOUT -> { layoutUpdates += numInstructions; i += numInstructions * 8 }
+          INSTRUCTION_UPDATE_PADDING -> { paddingUpdates += numInstructions; i += numInstructions * 5 }
+          INSTRUCTION_UPDATE_OVERFLOW_INSET -> { overflowUpdates += numInstructions; i += numInstructions * 5 }
+          INSTRUCTION_UPDATE_EVENT_EMITTER -> { eventEmitterUpdates += numInstructions; i += numInstructions }
+          else -> break
         }
       }
-      return s.toString()
+      val total = creates + deletes + inserts + removes + propUpdates + stateUpdates +
+          layoutUpdates + paddingUpdates + overflowUpdates + eventEmitterUpdates
+      return String.format(
+          Locale.ROOT,
+          "IntBufferBatchMountItem [surface:%d commit:%d] %d ops (create:%d delete:%d insert:%d remove:%d props:%d state:%d layout:%d padding:%d overflow:%d events:%d)",
+          surfaceId, commitNumber, total,
+          creates, deletes, inserts, removes, propUpdates, stateUpdates,
+          layoutUpdates, paddingUpdates, overflowUpdates, eventEmitterUpdates)
     } catch (e: Exception) {
-      // Generally, this only happens during development when a malformed buffer is sent through.
-      // In these cases, we print the buffers to assist in debugging.
-      // This should never happen in production, but if it does... it'd still be helpful to know.
-      FLog.e(TAG, "Caught exception trying to print", e)
-
-      val ss = StringBuilder()
-      var ii = 0
-      while (ii < intBufferLen) {
-        ss.append(intBuffer[ii])
-        ss.append(", ")
-        ii++
-      }
-      FLog.e(TAG, ss.toString())
-
-      var jj = 0
-      while (jj < objBufferLen) {
-        FLog.e(TAG, if (objBuffer[jj] != null) objBuffer[jj].toString() else "null")
-        jj++
-      }
-
-      return ""
+      return String.format(
+          Locale.ROOT,
+          "IntBufferBatchMountItem [surface:%d commit:%d intBuf:%d objBuf:%d] (toString failed: %s)",
+          surfaceId, commitNumber, intBufferLen, objBufferLen, e.message)
     }
   }
 
